import { style } from '@vanilla-extract/css';

export const cell = style({
  position: 'absolute',
  padding: '6px',
});

export const cellContent = style({
  padding: '6px',
  width: '100%',
  height: '100%',
});
