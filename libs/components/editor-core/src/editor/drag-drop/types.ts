export enum BlockDropPlacement {
    left = 'left',
    right = 'right',
    top = 'top',
    bottom = 'bottom',
    outerLeft = 'outer-left',
    outerRight = 'outer-right',
    none = 'none',
}

export enum GroupDirection {
    up = 'up',
    down = 'down',
}
