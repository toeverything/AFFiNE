export enum BlockDropPlacement {
    left = 'left',
    right = 'right',
    top = 'top',
    bottom = 'bottom',
    none = 'none',
}

export enum GroupDirection {
    up = 'up',
    down = 'down',
}
