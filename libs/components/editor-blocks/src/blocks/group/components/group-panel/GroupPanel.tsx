import { styled } from '@toeverything/components/ui';

const GroupPanel = styled('div')({
    height: 32,
    background: '#FFFFFF',
    display: 'flex',
    alignItems: 'center',
});

export { GroupPanel };
