---
description: AFFiNE iOS 移动端架构分析 - Onboarding 和 Settings 页面开发参考
globs:
  - packages/frontend/core/src/mobile/**
  - packages/frontend/apps/ios/**
  - packages/frontend/core/src/components/sign-in/**
---

# AFFiNE iOS 移动端架构

## 整体架构：Capacitor 混合应用

| 层级 | 技术栈 | 职责 |
|------|--------|------|
| Native Shell (Swift) | Swift + Capacitor | WebView 容器、原生插件注册、系统桥接 |
| Web App (React/TS) | React 19 + TypeScript + react-router-dom | 所有 UI、业务逻辑、路由 |

**Onboarding 和 Settings 的 UI 和逻辑全部在 React/TypeScript 层，不涉及 Swift 代码。**

---

## 核心文件结构

### Native Shell
- `packages/frontend/apps/ios/App/App/AffineViewController.swift` — Capacitor Bridge 主控制器
- `packages/frontend/apps/ios/src/index.tsx` — Web App 启动入口
- `packages/frontend/apps/ios/src/app.tsx` — React 根组件 (Router + Providers)

### 移动端核心 (`packages/frontend/core/src/mobile/`)
- `router.tsx` — 路由定义 (react-router-dom)
- `pages/` — 页面组件
- `components/` — 可复用组件
- `dialogs/` — 弹出对话框
- `modules/` — 服务模块 (haptics, navigation-gesture, search, virtual-keyboard)
- `views/` — 视图组件
- `hooks/` — 自定义 Hooks

---

## Onboarding（登录/注册）

### 当前流程
```
App 启动 → router.tsx → pages/index.tsx
  → 未登录 → /sign-in 或 SignInDialog 弹窗
    → MobileSignInPanel → SignInPanel (共享组件)
      → 状态机: signIn → signInWithEmail / signInWithPassword → authenticated
        → 回到首页 (home.tsx)
```

### 关键文件
| 文件 | 作用 |
|------|------|
| `mobile/pages/sign-in.tsx` | `/sign-in` 路由页面 |
| `mobile/components/sign-in/index.tsx` | `MobileSignInPanel` — 移动端登录面板 |
| `mobile/components/sign-in/layout.tsx` | 登录页布局（背景 + 安全区域） |
| `mobile/components/sign-in/background.tsx` | 登录页背景装饰 |
| `mobile/dialogs/sign-in/index.tsx` | `SignInDialog` — 弹窗形式的登录 |
| `core/src/components/sign-in/sign-in.tsx` | `SignInStep` — 核心登录表单（邮箱/OAuth/密码，桌面+移动共享） |
| `core/src/components/sign-in/index.tsx` | `SignInPanel` — 状态机管理多步登录 |

### 添加独立 Onboarding 页面的步骤
1. 在 `mobile/pages/` 下新增 onboarding 页面组件
2. 在 `mobile/router.tsx` 中注册新路由
3. 在 `pages/index.tsx` 或 `pages/root/index.tsx` 中添加首次启动检测逻辑

### 注意事项
- `components/sign-in/sign-in.tsx` 是桌面端和移动端共享的，修改时注意影响范围
- 如需仅移动端的变更，在 `mobile/components/sign-in/` 层处理

---

## Settings（设置）

### 当前结构
Settings 以 SwipeDialog 弹窗形式呈现（从底部滑出）。

### 入口
```
HomeHeader (views/home-header/index.tsx) → 齿轮图标
  → workspaceDialogService.open('setting', { activeTab: 'appearance' })
    → SettingDialog
```

### 关键文件
| 文件 | 作用 |
|------|------|
| `dialogs/setting/index.tsx` | **Settings 主入口** — 组合所有设置组 |
| `dialogs/setting/swipe-dialog.tsx` | 底部滑动弹窗容器 |
| `dialogs/setting/user-profile/` | 用户头像、昵称编辑 |
| `dialogs/setting/subscription/` | 订阅计划展示 |
| `dialogs/setting/user-usage/` | 云存储用量 |
| `dialogs/setting/appearance/index.tsx` | 外观设置入口 |
| `dialogs/setting/appearance/theme.tsx` | 主题切换 (亮/暗/跟随系统) |
| `dialogs/setting/appearance/font.tsx` | 字体选择 |
| `dialogs/setting/appearance/language.tsx` | 语言选择 |
| `dialogs/setting/about/index.tsx` | 版本信息、法律条款 |
| `dialogs/setting/experimental/` | 实验性功能开关 |
| `dialogs/setting/others/index.tsx` | 登出、缓存清理等 |
| `dialogs/setting/others/delete-account.tsx` | 删除账户 |
| `views/home-header/index.tsx` | 设置入口（齿轮图标点击） |

### Settings 组件组合 (MobileSetting)
```tsx
<UserProfile />
<UserSubscription />
<UserUsage />
<AppearanceGroup />
<AboutGroup />
<ExperimentalFeatureSetting />
<OthersGroup />
```

### 修改路径
- **增减设置项**：修改 `dialogs/setting/index.tsx` 中 MobileSetting 组件
- **修改某个设置组**：进入对应子目录修改
- **新增设置组**：在 `dialogs/setting/` 下新建目录 → 在 `index.tsx` 中引入
- **改变入口/触发方式**：修改 `views/home-header/index.tsx`

---

## 数据流与服务层

| 服务 | 来源 | 用途 |
|------|------|------|
| `AuthService` | `@affine/core/modules/cloud` | 登录状态、认证操作 |
| `ServerService` | `@affine/core/modules/cloud` | 服务器配置 |
| `WorkspaceDialogService` | `@affine/core/modules/dialogs` | 弹窗管理 |
| 用户设置 | GraphQL API (`UserSettingsType`) | 远程用户偏好 |
| 外观设置 | 本地存储 | 主题、字体、语言 |

## 路由定义 (mobile/router.tsx)
```
/                          → pages/index.tsx (默认首页)
/workspace/:workspaceId/*  → workspace 页面
/auth/:authType            → 认证页
/sign-in                   → 登录页
/magic-link                → 魔法链接登录
/oauth/*                   → OAuth 回调
```

## DI 框架
使用 `@toeverything/infra` 进行依赖注入：
- `useService(ServiceClass)` — 获取服务实例
- `useLiveData(observable$)` — 订阅响应式数据
- `FrameworkRoot` — DI 容器根
