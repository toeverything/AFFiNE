mutation changePassword(
  $email: String!
  $password: String!
  $oldPassword: String!
) {
  changePassword(
    email: $email
    password: $password
    oldPassword: $oldPassword
  ) {
    id
    name
    avatarUrl
    email
  }
}
