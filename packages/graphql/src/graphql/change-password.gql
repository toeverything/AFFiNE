mutation changePassword(
  $email: String!
  $password: String!
  $newPassword: String!
) {
  changePassword(
    email: $email
    password: $password
    newPassword: $newPassword
  ) {
    id
    name
    avatarUrl
    email
  }
}
