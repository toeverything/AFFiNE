mutation setBlob(
  $workspaceId: String!
  $blob: Upload!
  $anonymousGuestToken: String
) {
  setBlob(
    workspaceId: $workspaceId
    blob: $blob
    anonymousGuestToken: $anonymousGuestToken
  )
}
