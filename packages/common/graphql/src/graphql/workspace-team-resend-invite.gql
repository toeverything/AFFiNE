mutation resendWorkspaceTeamMemberInvite(
  $workspaceId: String!
  $inviteId: String!
) {
  resendMemberInvite(
    workspaceId: $workspaceId
    inviteId: $inviteId
  ) {
    allowed
    attempt
    retryAfterMs
    nextAllowedAt
  }
}
