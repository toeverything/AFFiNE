mutation claimAudioTranscription($jobId: String!) {
  claimAudioTranscription(jobId: $jobId) {
    id
    status
    transcription {
      speaker
      start
      end
      transcription
    }
    summary
  }
}
