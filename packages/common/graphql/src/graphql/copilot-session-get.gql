query getCopilotSession(
  $workspaceId: String!
  $sessionId: String!
) {
  currentUser {
    copilot(workspaceId: $workspaceId) {
      session(sessionId: $sessionId) {
        id
        pinned
        parentSessionId
        promptName
        model
        optionalModels
      }
    }
  }
}
