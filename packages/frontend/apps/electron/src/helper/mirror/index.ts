import {
  finalizeGeneration,
  inspectTarget,
  revealMirror,
  selectProjectDirectory,
  writeBatch,
} from './mirror';

export const mirrorHandlers = {
  selectProjectDirectory,
  inspectTarget,
  writeBatch,
  finalizeGeneration,
  revealMirror,
};
